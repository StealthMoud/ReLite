# ReLite Home keeps this minimal on purpose — no third-party analytics/crash
# SDKs to special-case. Add rules here only when a genuine obfuscation issue
# is found (e.g. via viewBinding or a reflection-based Android API).
